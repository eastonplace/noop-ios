# Canonical Strain V2 Plan

1. Restore the V2 scorer, provenance, and imported-source guards lost during backend reconciliation.
2. Pin shared physiological-day inputs and source ownership with failing tests.
3. Persist V2 directly for new NOOP-owned daily/workout rows.
4. Remove Home's mixed V1/V2 maximum and route every surface through canonical records.
5. Add a resumable full-history recompute cursor and targeted shadow cleanup.
6. Verify package, repository, report, Watch, and physical-device parity.

The performance fast paths in Spec 013 must preserve the corrected V2 outputs, not the superseded V1 shadow rollout.
