# Rollback 011

Revert the adoption commit as one release unit. Existing repositories, schemas,
persistence, and routes require no migration or downgrade. Do not uninstall the phone
app; install the last known-good signed build over it so user data remains intact.
